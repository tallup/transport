export default function ApplicationLogo({ className }) {
    return (
        <div className={className}>
            <span className="text-xl font-bold">Student Transport</span>
        </div>
    );
}

